{
    "version":"2.0.0",
    "tasks": [
        {
            "label": "Build and Pack Analyzer",
            "type": "shell",
            "command": "dotnet pack ${workspaceFolder}/InterfaceAnalyzer/InterfaceAnalyzer -c Release -o /local-nuget-feed -v diag",
            "problemMatcher": "$msCompile"
        }
    ]
}